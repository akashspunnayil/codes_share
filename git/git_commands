git config --global user.name "akashspunnayil"
git config --global user.email "akashspunnayil@gmail.com"
git config --global push.default current ! to avoid future "fatal: The current branch ferret has no upstream branch" error
git config -l

git init                                     !## personal token :  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


!Normal procedure of adding files
git checkout -b new-branch-name ! create new branch
git checkout -d branch-name ! delete a branch
git branch ! to make sure in new branch
git add folder/file-name
git add . ! to add all files in the current directory
git commit -m "commit message"
git push
git pull ! only if needed


git branch new-branch-name ! create new branch
git checkout new-branch-name ! to enter new branch 
git checkout -b new-branch-name ! create branch and enter 
git status ! check status of files, whether any files added or like that
git add specific-file ! adding specific file to checkouted branch
git rm specific-file ! remove a file from branch
git commit -m "type the message here" ! committing changes
git push -u origin new-branch-name ! push the branch to server

